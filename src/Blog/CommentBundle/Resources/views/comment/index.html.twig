{% for comment in comments %}
    <div>
        <p>
            {{ comment.content }}
        </p>
        <p>
            {{ comment.user.username }}
        </p>
        <p>
            {{ comment.createdAt|date('H:i') }}
        </p>
    </div>
    <hr>
{% endfor %}